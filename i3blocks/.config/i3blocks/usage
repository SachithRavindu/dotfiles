#!/bin/bash

# Get CPU usage (subtract idle %)
cpu=$(top -bn1 | grep "Cpu(s)" | awk '{printf "CPU %.1f%%", 100 - $8}')

# Get memory usage (used/total)
mem=$(free -h | awk '/^Mem:/ {printf "MEM " $3}')

# Print both
echo "$cpu | $mem"
